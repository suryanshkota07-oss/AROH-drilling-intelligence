import { ArrowDown, ShieldAlert } from 'lucide-react'
import type { Risk, Well } from '../../types/domain'
import { RiskBadge } from '../common/RiskBadge'
export function RiskAheadPanel({ risk, well }: { risk: Risk; well: Well }) { const startDepth = 2960; const distance = startDepth - well.currentDepth; return <section className="risk-ahead-panel" aria-labelledby="risk-ahead-title"><div className="risk-ahead-panel__heading"><span><ShieldAlert aria-hidden="true" size={23} /></span><div><p className="eyebrow">PRIORITY DECISION-SUPPORT SIGNAL</p><h2 id="risk-ahead-title">Risk Ahead</h2></div><RiskBadge level={risk.severity} /></div><div className="risk-ahead-panel__signal"><div><span>Predicted risk</span><strong>{risk.eventType}</strong></div><div className="risk-score"><span>Risk score</span><strong>{risk.riskScore}<small>/100</small></strong></div></div><div className="risk-ahead-panel__facts"><div><span>Predicted interval</span><strong>{risk.riskInterval}</strong></div><div><span>Distance ahead</span><strong><ArrowDown aria-hidden="true" size={14} /> {distance} m</strong></div><div><span>Confidence</span><strong>{risk.confidence}%</strong></div><div><span>Offset records</span><strong>{risk.evidenceCount}</strong></div></div><p className="risk-ahead-panel__note">Synthetic pattern signal only. Review supporting evidence and current conditions before any operational decision.</p></section> }
