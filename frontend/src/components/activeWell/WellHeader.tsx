import { Activity, MapPin, RadioTower } from 'lucide-react'
import type { Well } from '../../types/domain'
import { RiskBadge } from '../common/RiskBadge'
export function WellHeader({ well }: { well: Well }) { return <section className="well-header"><div className="well-header__identity"><span className="well-header__icon"><RadioTower aria-hidden="true" size={24} /></span><div><p className="eyebrow">CURRENT WELL · SYNTHETIC STREAM</p><h2>{well.name}</h2><p><MapPin aria-hidden="true" size={13} /> {well.location} · {well.wellId}</p></div></div><div className="well-header__metrics"><div><span>Current depth</span><strong>{well.currentDepth.toLocaleString()} <small>m</small></strong></div><div><span>Formation</span><strong>{well.formation}</strong></div><div><span>Drilling status</span><strong><Activity aria-hidden="true" size={14} /> {well.status}</strong></div><div><span>Data freshness</span><strong>{well.updatedAt}</strong></div><RiskBadge level={well.riskLevel} /></div></section> }
