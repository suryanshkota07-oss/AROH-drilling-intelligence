import { ClipboardCheck } from 'lucide-react'
import type { RecommendedAction as RecommendedActionType } from '../../types/domain'
export function RecommendedAction({ recommendation }: { recommendation: RecommendedActionType }) { return <section className="recommended-action" aria-labelledby="recommended-action-title"><div className="recommended-action__heading"><ClipboardCheck aria-hidden="true" size={21} /><div><p className="eyebrow">RECOMMENDED ACTION</p><h2 id="recommended-action-title">{recommendation.title}</h2></div></div><p className="recommended-action__action">{recommendation.action}</p><p className="recommended-action__rationale">{recommendation.rationale}</p><span className="review-notice">AI-generated decision support — requires engineer review</span></section> }
